{
  "jobs": [
    {
      "command": "0 0 * * 0-4 bundle exec ruby trigger_detached_run.rb --from-cron --app rdvsp"
    },
    {
      "command": "0 1 * * 0-4 bundle exec ruby trigger_detached_run.rb --from-cron --app rdvi"
    },
    {
      "command": "0 2 * * 0-4 bundle exec ruby trigger_detached_run.rb --from-cron --app rdvs"
    }
  ]
}
